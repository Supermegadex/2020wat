const greisch = [3, 0, 0, 0];

const golka = [15, 0, 0, 0];

const spanny = [12, 0, 0, 0];

const delkamiller = [13, 0, 0, 0];

const cotton = [24, 0, 0, 0];

const bauman = [10, 0, 0, 0];

const piernicky = [22, 0, 0, 0];

const sisson = [16, 0, 0, 0];

const mcGill = [5, 0, 0, 0];

export {
  greisch,
  golka,
  spanny,
  delkamiller,
  cotton,
  bauman,
  piernicky,
  sisson,
  mcGill
};
